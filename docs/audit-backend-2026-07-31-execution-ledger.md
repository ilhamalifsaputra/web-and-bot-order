# SDD ledger — plan: docs/superpowers/plans/2026-07-31-audit-backend-fixes.md
Task 1: minor (deferred): tokopay-webhook.test.ts:200 oldWrongExpectedCharge computed inline rather than via named helper — harmless, well-commented
Task 1: complete (commits 6cfdd7f..9e2637e, review clean)
Task 2: minor (deferred): no docstring note on pre-Money-2-fix legacy data anomaly (totalDiscount > orderSubtotal); no explicit single-line-no-discount test (covered implicitly by 17 unmodified passing tests)
Task 2: complete (commits 9e2637e..249dfd1, review clean)
Task 3: complete (commits 249dfd1..9d577ec, review clean)
Task 4: minor (deferred): create's max_discount and update's min_purchase in api/vouchers.ts remain unguarded against NaN/Infinity — legitimately out of brief's listed scope (no existing bound check to extend), future work if ever revisited
Task 4: complete (commits 9d577ec..6563a7a, review clean)
Task 5: fix round 1/5 (1 addressed, 0 open — leaked English in userWalletPrompt toast now routed through t(); commits e56ef90..e1843dd)
Task 5: complete (commits 6563a7a..e1843dd, review clean after 1 fix round)
Batch 1 (Tasks 1-5) complete.
Task 6: fix round 1/5 (1 addressed, 0 open — added LEAK_EMAIL/email-key assertions to all 6 H-4 tests; commits 6b59e6b..0927841)
Task 6: complete (commits e1843dd..0927841, review clean after 1 fix round)
Task 6: recommended follow-up (not fixed, out of scope): api/reviews.ts GET /api/reviews spreads listReviews' include:{user:true} result, leaking reviewer passwordHash/email to any readonly admin; api/search.ts searchUsers has no select at all (fix there must keep email for SearchModal.tsx's identity fallback while dropping passwordHash, can't reuse USER_SELECT)
Task 7: fix round 1/5 (2 addressed, 0 open — plan-mandated fix (checkSetupLock) was factually wrong for this route's real caller; corrected to currentAdmin guard after controller diagnosed the auto-login-before-Done-screen flow; tests rewritten to drive the real wizard-completion path instead of a synthetic mid-wizard state; commits 4f94cf6..b3c30ef)
Task 7: complete (commits 0927841..b3c30ef, review clean after 1 fix round)
Task 7: recommended follow-up (not fixed, documented in plan file): /setup/restart still has no CSRF protection (client doesn't send a token); consider adding csrfProtect + updating SetupDonePage.tsx to attach one, or retiring /setup/restart entirely in favor of the already-CSRF-protected POST /api/settings/restart (api/settings.ts:448-467)
Task 8: minor (deferred): client-side CSRF-meta staleness after password change (web-admin SettingsPage.tsx doesn't reload/refresh csrf meta unlike storefront's equivalent) — degrades gracefully (friendly "reload" message, no logout, no loop), fixed by manual reload; consistent with Task 7's precedent of deferring client-parity gaps
Task 8: complete (commits b3c30ef..89a13b5, review clean)
Task 9: minor (deferred): shared per-IP rate-limit bucket across /auth/login,/auth/forgot,/auth/register,/auth/reset — pre-existing pattern, matches brief's literal text, not a regression; report noise: implementer's report cited a brief parenthetical ("per-IP registration cap distinct from login window") that does not exist anywhere in the brief/plan/repo — report-accuracy issue only, code itself is correct
Task 9: complete (commits 89a13b5..428a5e7, review clean)
Task 10: minor (deferred): export TICKET_DIR placed separately from declaration (style only); no explicit readdir-diff test for mid-parse ValidationError paths (bad mimetype/oversized/too-many-files) beyond empty-message/ownership-failure — low-risk since single-write-call-site architecture makes those paths structurally incapable of writing, verified by code inspection
Task 10: complete (commits 428a5e7..7c50c38, review clean)
Task 11: complete (commits 7c50c38..fc1d2d4, review clean, zero findings)
Task 12: minor (deferred): void vs bare reply.header() style inconsistency between storefront/web-admin (matches each file's own local convention, not a defect); spaFallback.ts (error/setup-gate pages) has no Cache-Control at all but never carries a live CSRF token so M-20 doesn't apply there — general cache-hygiene follow-up, not a leak
Task 12: complete (commits fc1d2d4..cab9570, review clean)
Batch 2 (Tasks 6-12) complete.
Task 13: minor (deferred): assertNotPaidWithoutCredit's "already-credited escape hatch" is currently unreachable dead code (creditOrderToBalance already moves order to CANCELLED before this guard would fire) — harmless, matches brief's literal wording, worth a comment noting it's defense-in-depth not load-bearing
Task 13: complete (commits cab9570..7a9ef85, review clean)
Task 14: complete (commits 7a9ef85..f0eeb7b, review clean, zero findings). Note: original implementer subagent hit a session limit mid-task after fixing all 5 production files and writing 3/5 test files; controller completed the remaining 2 test files (binance-internal.test.ts, bybit-deposit.test.ts) by transcribing the same proven pattern, ran full verification, and committed. Reviewer independently confirmed test-rigor parity between implementer-written and controller-completed tests — no shortcut taken.
Task 15: fix round 1/5 (1 addressed, 0 open — deliveryTypeSnapshot made nullable with live-value fallback in settlePaidOrder so a db-push deploy boundary can't mis-snapshot pre-existing rows; reviewer independently applied the migration SQL to a real SQLite DB and verified the deploy-boundary reasoning holds; commits 8d2becf..fb6cdd9)
Task 15: complete (commits f0eeb7b..fb6cdd9, review clean after 1 fix round)
Task 15: recommended follow-up (not fixed, out of scope): apps/web-admin/src/routes/api/orders.ts ~273,512 (resend-DM feature, single+bulk) still read live product.deliveryType instead of the new deliveryTypeSnapshot — would need the same null-fallback treatment if picked up; same shape (lower impact, display/keyboard only) in apps/order-bot/src/handlers/customer.ts:826 and keyboards/customer.ts:403
Task 16: minor (deferred): generic error.generic message on lost claim doesn't point buyer directly to "My Orders" (only "Main Menu") — cosmetic, doesn't strand buyer, consistent with implementer's judgment call
Task 16: complete (commits fb6cdd9..41f9354, review clean)
Task 17: minor (deferred): performWalletCheckout has no pre-transaction fail-fast of its own (relies on createOrderFromCart's defense-in-depth check, which fires before any expensive work so the underlying vulnerability isn't reopened — just an asymmetry); getCart read up to 3x per checkout (harmless, O(lines) not O(units))
Task 17: complete (commits 41f9354..4dabd3d, review clean)
Task 18: complete (commits 4dabd3d..2b701d5, review clean, zero findings)
Batch 3 (Tasks 13-18) complete.
Task 19: complete (commits 2b701d5..7eb1172, review clean, zero findings)
Task 20: fix round 1/5 (2 addressed, 0 open — ADMIN_STALE_PAYMENT registered in ADMIN_DM_EVENTS (was previously silently dropped or leaked to public channel), trxId capped at 300 chars; controller completed the fix directly after the resumed implementer subagent hit a session limit before making any change; commits a550a17..296abec)
Task 20: complete (commits 7eb1172..296abec, review clean after 1 fix round)
Task 21: minor (deferred): recordBybitBscTrackingStale's reason parameter no longer persisted to any DB column (previously via OrderStatusHistory.meta on the FAILED transition) — only used for log line/alert payload, accepted scope tradeoff, no natural non-status-change history column exists
Task 21: complete (commits 296abec..0f9b4e4, review clean)
Task 22: fix round 1/5 (1 addressed, 0 open — guard now also rejects literal empty-string payment_id via body.payment_id === "" alongside the typeof check, without over-broadly rejecting legitimate "0"/0 ids; commits d98e212..31e8664)
Task 22: complete (commits 0f9b4e4..31e8664, review clean after 1 fix round)
Task 23: complete (commits 31e8664..01225f4, review clean, zero findings)
Task 24: fix round 1 in progress — reviewer found 3 Critical design flaws in the initial implementation (afb8d49): (1) asymmetric filter made exact payments unmatchable whenever a cheaper pending order exists, defeating the unique-cents disambiguation mechanism entirely in production; (2) unbounded overpayment accept lets an unrelated large deposit auto-deliver against a tiny pending order with no flag; (3) floor-less underpaid matching lets a stray small deposit knock a legitimate pending order into UNDERPAID, orphaning the buyer's real payment. Dispatched substantial rework: best-fit-with-tie-refusal matching, an overpayment cap, an underpaid floor, plus atomicity fix for the new markUnderpaid* writes (Important #5) and a production-shape test (distinct-total multi-order exact match, Important #4).
Task 24: fix round 1/5 (5 addressed, 0 open — best-fit matching with tie-refusal replaces the asymmetric filter, overpaymentCap(total)=max(2,20%) added, UNDERPAID_FLOOR_PERCENT=0.5 added, markUnderpaidBybit/markUnderpaidBybitBsc wrapped in $transaction for atomicity, production-shape distinct-total multi-order test added on both rails; commits afb8d49..79dc618)
Task 24: complete (commits 01225f4..79dc618, review clean after 1 fix round)
Batch 4 (Tasks 19-24) complete except Task 25 (M-15) still pending.
Task 25: complete (commits 79dc618..99c2876, review clean, zero findings)
Batch 4 (Tasks 19-25) complete.
Task 26: minor (deferred): report claimed 4 new reviews.test.ts tests, diff shows 2 (5 total new incl. 3 handler tests) — report-accuracy only; deleteRestockSubscription throwing post-send-success would misleadingly log "kept for retry" causing a possible duplicate DM on next restock — extremely low-probability, matches drainBroadcasts's existing lack of send/bookkeeping-failure distinction, not a regression
Task 26: complete (commits 99c2876..6bbdcf2, review clean)
Task 27: fix round 1 dispatched — reviewer approved the core two-phase architecture (short claim transaction + chunked non-transactional enqueue) as the right trade-off for H-7, but found 2 Important defects in the implementer's own self-disclosed mitigation: (1) notifications.ts:584-604 broadcast.create runs only after the full chunk loop succeeds, so a partial failure leaves ZERO Broadcast History row even though earlier chunks already committed real outbox rows/DMs — contradicting the catch-block's own "check Broadcast History" guidance; (2) the documented recovery path (reschedule the SKU) triggers a full fresh fan-out with no dedup against already-sent chunks, risking duplicate DMs to customers already reached. Dispatching fix to the same implementer.
Task 27: fix round 1/5 (1 addressed, 1 accepted-as-authorized, 1 new open — Finding 1 (empty Broadcast row on partial failure) ADDRESSED via pre-loop Broadcast row + incremental sentCount + FAILED flip; Finding 2 (dup-DM recovery risk) not technically resolved but this was the coordinator-authorized fallback from round 1's dispatch ("if dedup out of scope, make the warning honest") — parked as accepted, not re-opened; NEW Important breakage introduced by the fix itself: Broadcast.status can get stuck in SENDING forever if the trailing SENDING->SENT update fails/times out (unguarded by try/catch, and claimedAt is never set so reapStaleBroadcasts's stale-claim reaper can't catch it either) — same-root-cause double-fault risk if the FAILED-flip update in the catch block also throws; commits deab713..2bb493d)
Task 27: fix round 2/5 (1 addressed, 0 open — claimedAt: new Date() set on Broadcast row creation so reapStaleBroadcasts can reclaim a permanently-SENDING row; FAILED-flip failure isolated to its own try/catch so it can't mask the original chunk error; re-review traced reapStaleBroadcasts end-to-end and confirmed reclaiming is a pure status-only operation with no retry/resend path, so no duplicate-DM risk was introduced; SENT-flip-failure mislabeling accepted as cosmetic Minor since nothing keys off FAILED to trigger a resend; commits 2bb493d..da0c4c4)
Task 27: complete (commits 6bbdcf2..da0c4c4, review clean after 2 fix rounds)
Batch 5 in progress: Tasks 26-27 complete.
Task 28: INCIDENT — implementer subagent's git commands ran against the main repo working directory (master) instead of this worktree, landing commit aa982a3 on master at tip 6cfdd7f despite explicit worktree-path instructions in the dispatch prompt. Diagnosed via git reflog (aa982a3 absent from worktree reflog) + git branch --contains (master only). Content verified legitimate (2 files, +69/-0, matches brief) via git show --stat before any corrective action. Recovered by cherry-picking aa982a3 onto worktree-fix-backend-audit-2026-07-31 as 7f72f02 (clean cherry-pick, no conflicts), then — with explicit user confirmation — git reset --hard 6cfdd7f on master in the main repo to remove the stray commit (safe: clean working tree, single commit at tip, no dependents). Continuing review against 7f72f02.
Task 28: minor (deferred): logger.warn+answerCallbackQuery pair duplicated verbatim 10x in admin.ts — consistent with existing dispatchBrowse/callbacks.ts precedent of inline duplication rather than a helper, not a new style; task-28-report.md was also written to the wrong directory (main repo instead of worktree) by the same haiku-model implementer that mis-targeted its commit — recovered by copying it into the worktree's SDD workspace, stray dir removed from main repo
Task 28: complete (commits da0c4c4..7f72f02, review clean; commit recovered from a stray-directory incident, see INCIDENT note above)
Task 29: recommended follow-up (not fixed, plan-level gap, not this task's defect): M-22's brief only scoped admin.ts's stockUploadConversation (lines 111-134) even though the audit's own Problem statement is general ("every conversation.wait() loop"). Reviewer independently confirmed at least 8 more admin.ts conversations share the identical if(handledEscape)/if(!message.text continue) shape (implementer's own flag), PLUS productCreateConversation's type-picker (~712-726) and draft-resume (~660-676) loops and broadcastConversation's message-wait (~360-379) and confirm/cancel (~390-402) loops share the same missing-toast defect via a different no-continue fall-through shape. Worth a dedicated follow-up task enumerating admin.ts wait loops by shape, not by textual pattern-match, once this plan's 47 tasks are done.
Task 29: complete (commits 7f72f02..1a63f85, review clean)
Task 30: fix round 1 dispatched — Critical finding: all 3 new menuAnchor calls (support.ts:70,99,140) pass the stale entry-time `ctx` instead of the freshest waited context `u`, diverging from the established post-wait convention every other menuAnchor/adminAnchor wizard in this codebase follows (checkout.ts, customerInfo.ts, editCustomerInfo.ts all use `u` post-wait). Reviewer traced @grammyjs/conversations' replay/session-snapshot machinery to show this is a real correctness risk, not style: on editAnchor's fresh-send fallback path (anchor message not editable), the new anchor id written to ctx.session.menuMsgId would land on a replayed/historical context object rather than the live persisted session, silently resurrecting the exact stray-un-retired-keyboard bug this task exists to fix. Test can't catch this because FakeConversation gives every scripted context its own independent session object, not modeling grammY's real replay semantics. Dispatching fix to the same implementer.
Task 30: fix round 1 BLOCKED mid-flight on a genuine technical conflict — implementer subagent hit a session limit after writing the fix but before testing/committing, leaving it UNCOMMITTED in the working tree (support.ts, +16/-3: adds `let lastCtx: MyContext = ctx` before each of the 3 wait loops, assigns lastCtx = u at the top of each iteration, passes lastCtx to all 3 menuAnchor calls). Controller ran typecheck (clean) and the scoped test — and the supposedly-correct `u` pattern FAILS the M-23 test written in round 0 (expects 1 reply, got 2: the 2nd menuAnchor found no tracked anchor id and fresh-sent instead of editing), because test/helpers/ctx.ts's makeCtx gives every scripted context its own independent session object while menuAnchor both reads AND writes ctx.session.menuMsgId. So under the fake harness the reviewer's "incorrect" ctx pattern passes and the "correct" u pattern breaks. Dispatched a dedicated opus investigation (read-only) into @grammyjs/conversations' actual session-across-wait semantics in node_modules + this repo's session wiring + whether customerInfo/editCustomerInfo tests genuinely prove the u pattern works, to determine whether (1) u is right and the harness needs fixing, (2) ctx is right and the Critical finding is wrong, or (3) conversation.session is the real answer. DO NOT commit or revert the working-tree change until that verdict lands.
Task 30: INVESTIGATION VERDICT (full writeup: .superpowers/sdd/2026-07-31-audit-backend-fixes/task-30-investigation.md) — `u` (freshest waited context) is CORRECT; reviewer's Critical finding upheld; the TEST HARNESS is the inaccurate model, not the production fix. Evidence from @grammyjs/conversations@1.2.0 source: _replayWait() (conversation.js:446-473) returns the REAL live ctx only when not replaying (:455) and a FAKE Object.assign(new Context(...), x) reconstruction otherwise (:459) whose session is a turn-1 deep-clone snapshot; only handle.ctx.session (== the last wait()'s u.session) is persisted by grammY's session() finish() (session.js:224-236); conversation.session is literally currentCtx.session == u.session (:470,:821-829), not a third option. Writes to a replayed ctx's session are worse than lost — Object.assign copies x.session BY REFERENCE and x lives in the op log that conversations() re-listifies into the persisted session (:273-278), i.e. it rewrites replay history, which the plugin explicitly forbids (:806-829). Concrete production breakage with entry ctx: editAnchor's fresh-send fallback (chat.ts:230-232) writes the NEW anchor id into the throwaway clone, so the live session keeps a dead id and the new bubble keeps a live never-retired keyboard — exactly the M-23 stray-keyboard class. Repo wiring confirmed stock (main.ts:64-79 plain session(), not lazySession, no proxy). No existing test caught it because customerInfo/editCustomerInfo tests only assert the waited context's OWN session and never assert reply/edit counts or cross-wait anchor continuity. Dispatched opus fix round 2: keep production fix, add sharedSession option + deferred (thunk) FakeConversation queue to test/helpers/ctx.ts, rewrite M-23 assertions from mechanism to invariant (one reply ever, anchor id never moves), add the missing discriminator test (force editAnchor fresh-send fallback; must fail with ctx, pass with lastCtx — implementer instructed to verify both directions empirically).
Task 30: fix round 2/5 (1 addressed, 0 open — production fix kept (lastCtx reassigned as the FIRST statement of all 3 wait loops, verified no continue/break path skips it; all 3 post-wait menuAnchor + paired t() calls use lastCtx); harness corrected additively (optional sharedSession, FakeConversation queue widened to MyContext | (() => MyContext) with thunk resolution, new deletedMessageIds option + api-level recApiEdit rejecting on deleted target to mirror real Telegram's "message to edit not found"); M-23 test rewritten to invariant assertions; NEW discriminator test added and verified BOTH directions empirically (with fix: pass; production fix stashed: AssertionError expected 500001 not to be 500001 at conversations.test.ts:432). Re-reviewer independently confirmed the discriminator is structurally real (traces menuAnchor's ctx.callbackQuery branch: with fix -> typed-input path -> api-level edit -> rejected -> fresh-send fallback -> writes live session; with bug -> entry ctx still has callbackQuery -> smartEdit ctx-level edit -> succeeds -> writes throwaway entrySnapshot), audited all production deleteMessage call sites for harness-change blast radius (none chain into an edit on the same id), and confirmed 469->470 test count means no existing test semantics changed; commits bba6e50..923b9eb)
Task 30: complete (commits 1a63f85..923b9eb, review clean after 2 fix rounds)
Task 30: minor (deferred): the rewritten M-23 invariant test is not itself a discriminator (with the shared session it passes either way) — the separate regression test carries the cover, so a future edit removing the discriminator while trusting the invariant test would silently reopen the bug class; worth a one-line cross-reference comment. Also the harness models the entry ctx as a FULLY detached session (real divergence starts at turn 2, papered over by pre-seeding both with the same anchor id) — faithful for this bug class, would need a different model for turn-1-specific divergence.
Batch 5 in progress: Tasks 26-30 complete.
Task 31: complete (commits 923b9eb..e7e70fc, review clean, zero findings — reviewer independently traced that handleQtyTextInput is only ever reached via text-message paths so menuAnchor's callbackQuery short-circuit is dead code there, and verified the new test would genuinely FAIL against pre-fix 923b9eb rather than passing vacuously)
Task 32: complete (commits e7e70fc..170bd59, review clean) — implementer reused resolveSegmentRecipients(prisma,"ALL") instead of a new crud helper; reviewer independently verified segmentWhere("ALL") is exactly { banned: false, telegramId: { not: null } } with no other filtering, i.e. the new recipient set is a STRICT SUBSET of the old (no silent targeting change), and confirmed the brief's "both queries" phrasing was inaccurate — only one findMany existed, read twice. Coupling to BroadcastSegment "ALL" semantics is real but explicitly documented in a code comment (admin.ts:49-52) + report; accepted as correct over premature near-duplicate abstraction.
Task 32: minor (deferred): resolveSegmentRecipients selects language but the ad-hoc broadcast conversation never uses it (harmless unused-field fetch); the "ALL"-semantics coupling would silently follow if web-admin ever adds an opt-out flag to that segment.
Task 33: complete (commits 170bd59..a2e815c, review clean, zero findings) — all 4 Cron registrations verifiably carry { protect: true }; flag-write moved before the DM loop in all 3 watchdogs; reviewer read reconcileFinancesJob's body directly and confirmed it genuinely has NO alert-flag pattern (recomputes fresh each 6h run, no getSetting/setSetting dedupe) so protect-only was correct, not an oversight; per-admin DM failures ARE individually caught/logged so the trade-off comment is accurately scoped to process-level failure; croner's protect lock is untestable from a unit test (pre-authorized substitution) but the substitute tests are genuine — Test 1 nests a second watchdog call inside the first admin's sendMessage to prove the FLAG (not the scheduler lock) blocks the overlap, Test 2 injects a mid-loop DM rejection and asserts the flag still lands + next tick sends zero; reviewer traced settings.ts:39-45 to confirm setSetting updates its in-memory cache synchronously so the write-then-read ordering is real, not mocked.
Task 34: complete (commits a2e815c..7b4f776, review clean after controller adjudication) — entire loop-iteration body now inside try/catch mirroring autoCancelExpiredOrders' exact outer(state-change)/inner(DM) shape; reviewer verified getUser(prisma, ticket.userId) is a TRUE drop-in for the raw findUnique (same id key, same null-on-not-found, USER_SELECT is a superset of the telegramId/language the loop reads, no N+1); test proves real batch continuation via DB-state assertions (failing row still REPLIED, healthy row actually CLOSED, exactly one DM to the survivor), not mock-call counting.
Task 34: ADJUDICATED (controller overruled reviewer's one Important finding) — reviewer flagged the implementer's report as misattributing the dbBytes flake to "the task instructions" since task-34-brief.md doesn't mention it. The reviewer was working from the brief file only and cannot see controller dispatch prompts: the dbBytes/VouchersPage/order_creation/web.test flake list WAS explicitly named in this task's dispatch prompt, so the implementer's citation was accurate and the finding is a false positive born of the reviewer's missing context. No fix dispatched. Note for future reviews: reviewers see the brief, not the dispatch, so dispatch-only context can surface as phantom report-accuracy findings.
Task 35: complete (commits 7b4f776..941ddaa, code verified clean; one Important report-accuracy finding ADJUDICATED by controller correction rather than a fix round, since no code defect was involved). Reviewer verified all substantive points: credential log is count-only with no metadata loophole (live test run confirmed the emitted line), 4 new locale keys parity-clean in both en/id with genuine Indonesian values (locales.test.ts 2/2 pass), and maybeAlertLowStock IS genuinely per-recipient (getUserByTelegramId + langCode called INSIDE the per-adminId loop, not resolved once and reused).
Task 35: CONTROLLER RULING on the Important finding — the implementer's report claimed admin.ts:200-203 had "drifted ~300+ lines due to Tasks 26/28" and substituted the Support Tickets header at :576 as "the equivalent target". Reviewer traced the real history and disproved this: at the true audit baseline 6cfdd7f, admin.ts:200-203 was userWalletPrompt's hardcoded /wallet usage string, which was ALREADY localized by e1843dd (Task 5's M-4 follow-up) long before Task 35; at this task's base 7b4f776 it sat at 197-203, a ~2-line shift. So the reference failed to match because it was already fixed, not because it moved. The :576 fix is legitimate but is VOLUNTARY OUT-OF-BRIEF SCOPE, not M-28's fifth location. Ruling: code stands (the extra fix is harmless and correct), no fix round dispatched (report-only defect), and a CONTROLLER CORRECTION section was appended to task-35-report.md so the record isn't misleading. M-28 closure status: all 5 audited locations resolved — 4 by this task, the 5th by e1843dd — plus 1 voluntary extra.
Task 35: minor (deferred): maybeAlertLowStock resolves each admin's language inside the nested for(denomination){for(adminId)} loop, so a run with multiple low-stock denominations re-looks-up the same admin's language once per denomination — harmless (small admin count, rare alert path), worth hoisting if revisited.
BATCH 5 COMPLETE (Tasks 26-35: H-6, H-7, M-21 through M-28). Next: Batch 6 (Tasks 36-46 — data layer and schema: H-8, H-9, M-29 through M-37).
Task 36: fix round 1 dispatched — reviewer (opus) found a REPRODUCED Critical data-loss hazard, not a theoretical one. Mechanical deliverables all verified good (zero drift confirmed independently via migrate diff --exit-code, timestamp 20260801000000 > 20260731120000, all 9 audit-named columns + 2 named indexes + 4 legitimate branch extras present, column enumeration in both rebuilds provably complete and correctly ordered, coalesce backfill for the last_status_change_at NOT NULL tightening correct, indexes/uniques faithfully recreated, types/DEFAULTs preserved). BUT: this is the first migration in repo history to rebuild PARENT tables. SQLite's PRAGMA foreign_keys=OFF is a documented no-op INSIDE a transaction, and defer_foreign_keys defers constraint CHECKING but does NOT suppress FK ACTIONS — so DROP TABLE's implicit DELETE fires ON DELETE CASCADE. denominations is parent to 5 CASCADE children (bulk_pricing, cart_items, restock_subscriptions, reviews, stock_items); support_tickets is parent to ticket_messages. Reviewer reproduced on a throwaway node:sqlite DB with this migration's exact PRAGMA sequence: outside a transaction children survive (3 stock_items, 2 reviews); inside BEGIN/COMMIT children go to ZERO with NO error raised. Also: the migration header asserts unconditional data-preservation safety (false, and would encourage running it on populated data), and 20260725000000_add_ticket_priority_category_resolved's own comment explicitly warned "rather than a full table rebuild: support_tickets carries live production data" — this commit does exactly that rebuild without acknowledging it. Prescribed fix: move the 6 mid-model fields to the END of the Denomination model in schema.prisma (cosmetic in Prisma) so migrate diff emits plain ADD COLUMN and the denominations rebuild disappears entirely; support_tickets' rebuild is unavoidable (nullable->NOT NULL can't be done via ALTER TABLE in SQLite) so it needs an honest operational warning instead of a false safety claim. Plus Important: the drift check only lives in a workflow_dispatch-only CI file so it enforces nothing today (suggest pretest script), and docs/MIGRATIONS.md still carries the false byte-identical claim the audit flagged.
Task 36: fix round 1/5 (6 addressed, 0 open — Critical 1 ELIMINATED not documented: denominations rebuild replaced with 14 plain ALTER TABLE ADD COLUMN / CREATE INDEX statements, grep confirms no DROP TABLE/RedefineTables/new_denominations anywhere in 20260801000000; Critical 2 isolated into its own migration 20260801000001_support_tickets_last_status_change_not_null with the prior 2026-07-25 decision explicitly RE-ARGUED rather than silently reversed; Critical 3 header rewritten to claim only what's true per-file; Important 4 pretest hook added to package.json:24 and re-reviewer empirically verified pnpm 9.15.9 runs pretest before test AND that the drift command works with DATABASE_URL_PRISMA unset so a fresh clone won't break; Important 5 docs/MIGRATIONS.md:18-31 corrected to admit the byte-identical claim held only for some batches and cite H-8 as the disproof; Minor 6 new FK documented; commits be1da7a..813e3e7)
Task 36: IMPLEMENTER PUSHBACK UPHELD — the round-1 reviewer's prescribed fix ("move the 6 fields to the end of the Denomination model") was WRONG and the implementer was right to reject it. Re-reviewer reproduced in a scratch project: a new NOT NULL + @default column forces a RedefineTables rebuild regardless of declared position (deliveryType is String @default("auto")), while a nullable no-default column gets a plain ADD COLUMN either way. Credit to the implementer for testing the prescription instead of following it.
Task 36: ACCEPTANCE TEST verified independently by re-reviewer inside the repo (with a before/after guard on data/, which contains only uploads/ in this worktree — no .db file exists here): `prisma migrate diff --from-migrations ./prisma/migrations --to-schema-datamodel ./prisma/schema.prisma --exit-code` -> "No difference detected.", EXITCODE=0. Also confirmed --script returns "-- This is an empty migration." The round-1 reviewer's warning that hand-written ADD COLUMNs would reintroduce drift did NOT materialize.
Task 36: RESIDUAL HAZARD ACCEPTED (support_tickets rebuild in 20260801000001) — genuinely unavoidable (SQLite has no ALTER COLUMN for the nullable->NOT NULL tightening, and the missing order_id FK also can't be added any other way). Re-reviewer independently reproduced all four cases on scratch DBs: migrate deploy -> children survive (tickets=2 messages=3, NULL correctly backfilled); db push -> children survive; FK-enforcement sanity check confirms cascade was genuinely live; explicit BEGIN/script/COMMIT via node:sqlite -> tickets=2 messages=0, ALL CHILDREN SILENTLY DELETED, zero errors. Verdict: acceptable to ship because the repo's actual deploy path (db push) never reads this file, the file is isolated so it can't ride along with the safe part, and the hazard is documented prominently (migration.sql:13-25 pragma-inside-transaction no-op with SQLite doc link, :26-40 the four-mechanism reproduction, :41-52 an explicit "DO NOT do that with this file"). Residual: an operator pasting it into a GUI client with "run as transaction" enabled would still lose ticket_messages.
Task 36: SEQUENCING CONFIRMED — closing the lastStatusChangeAt nullable->NOT NULL gap here (rather than deferring to Task 38/M-29) is coherent: deferring would have left this task's own new drift check permanently red. Re-reviewer verified Task 38's remainder is clean and still meaningful — the write-path half is untouched: crud/support.ts:181-189 (replyToTicket), :142-145 (reopenTicket), :104-106 (closeTicketByUser) all still fail to stamp lastStatusChangeAt. Post-fix they leave a STALE value rather than NULL, same bug, unchanged in nature. Cross-task handoff note left at 20260801000001/migration.sql:67-75.
Task 36: complete (commits 941ddaa..813e3e7, review clean after 1 fix round)
Task 36: minor (deferred, FOR FINAL-REVIEW FIX WAVE — actively misleading committed comments worth correcting): (1) prisma/schema.prisma:573-578 duplicates the lastStatusChangeAt doc comment's first three lines verbatim (old comment not removed when new one added); (2) schema.prisma:596-602 claims closedAt triggers the support_tickets rebuild due to "field order not matching true physical column order" — re-reviewer DISPROVED this causality and identified the real independent driver: the missing support_tickets_order_id_fkey (schema.prisma:611 declares the relation but 20260724150000 added order_id as a bare column with no FK, and adding a FK requires a rebuild); proof: deleting the order/tickets relation pair makes the same diff emit a plain ADD COLUMN with no rebuild; (3) schema.prisma:203-207 claims physical column order "matters for any FUTURE nullable/no-default column" — disproved, Prisma's SQLite differ ignores column order entirely; this comment would steer a future contributor into a pointless field-reordering ritual. Also: 20260801000001/migration.sql:53-66 files the new order_id FK under "ADDITIONAL RISK (not cascade-related)" implying it's incidental, when it is actually a REQUIRED outcome (without it the drift check can't go green) — worth a one-line correction so nobody "cleans up" the FK and reopens H-8. Nit: coalesce(..., "created_at") would be more faithful than CURRENT_TIMESTAMP at 20260801000001/migration.sql:107 for chronology preservation.
Task 37: review APPROVED with 2 Important follow-ups entering fix round 1. Central claim INDEPENDENTLY REPRODUCED THREE WAYS by the reviewer and found to be understated, not overstated: (1) primitive isolation via prisma db execute — bare coalesce("missing_col",...) writes the literal string "missing_col" and reports success, qualified coalesce("src"."missing_col",...) errors "no such column"; (2) old vs new migration file against a half-applied support_tickets — OLD reported success while writing last_status_change_at="last_status_change_at" on every row, NEW failed loudly with the source table untouched and ticket_messages intact; (3) full chain through REAL migrate deploy, rebuilding scenario 3 from scratch (deploy to 20260725000000_add_support_ticket_priority, seed, restore the original broken file from 058afd7^, deploy -> genuine P3018 with last_status_change_at absent and applied_steps_count=0), then resolve --applied + deploy: with OLD file "All migrations successfully applied" + silent corruption on every row; with NEW file P3018 loud abort, zero data changed. Reviewer also confirmed: the qualified form aborts BEFORE DROP TABLE so source + FK children survive; DROP TABLE IF EXISTS "new_support_tickets" is load-bearing not padding (proved retry succeeds with it, fails "table already exists" without it) and cannot drop anything real; no regression on healthy data (distinct values preserved exactly, NULL correctly coalesced); acceptance gate green (migrate diff "No difference detected." EXIT=0).
Task 37: reviewer's MECHANISM CORRECTION — the original ADD COLUMN ... NOT NULL DEFAULT CURRENT_TIMESTAMP SUCCEEDS on an empty table and only fails on a populated one, so the implementer's seed-first reproduction method is what makes it valid; the reviewer's own first unseeded attempt briefly looked like a refutation. docs/MIGRATIONS.md should state this or a future operator will conclude the bug is fictional.
Task 37: RENAME/IN-PLACE DECISION UPHELD — reviewer independently confirmed the checksum argument (migrate deploy verifies checksums of applied migrations, comments included), confirmed SQLite genuinely cannot make the file re-runnable (no ADD COLUMN IF NOT EXISTS; reproduced "duplicate column name: category" via resolve --rolled-back + deploy), and confirmed the collision is inert not just empirically but by reading both files: add_support_ticket_priority touches only priority + its index, add_ticket_priority_category_resolved touches category/first_response_at/resolved_at/last_status_change_at + ix_support_tickets_status and explicitly declines to re-add priority — disjoint sets, no read-after-write, order cannot matter. Forward-only + labelled rename proposal is the right call.
Task 37: three-scenario analysis VERIFIED (not merely reasoned) — scenario 3 handled in code, and both documented recovery orders run end-to-end to an identical correct final state (repair -> resolve --applied -> deploy; and from the aborted state: repair -> resolve --rolled-back 20260801000001 -> deploy). docs/MIGRATIONS.md repair SQL confirmed a byte-faithful completion of the corrected file's remaining statements; P3018/P3009 semantics table and the "resolve only touches _prisma_migrations" claim both check out against observed behavior.
Task 37: fix round 1/5 (2 Important + 2 Minor addressed, 0 open; commits bb82ec1..fd38d7a). Implementer wrote all the code but hit a session limit immediately before running the acceptance gates and committing; controller verified, ran every gate, ran the required negative tests, and committed (same pattern used for Tasks 14 and 20 — controller did NOT author any of the fix). Changes: check-migration-timestamps.ts GRANDFATHERED now maps a timestamp to its EXACT expected folder set and requires set equality (Important 1) + folder/timestamp counts reported separately (Minor 3); NEW scripts/check-migration-rebuild-quoting.ts rejects the unqualified-column rebuild idiom in any migration, grandfathering the two checksum-frozen pre-H-9 folders by exact folder name PLUS a pinned count of expected unqualified rebuilds so editing a frozen migration can't pass unnoticed either, and flagging stale allowlist entries (Important 2); both chained into pretest alongside the drift check and added to the CI workflow; docs/MIGRATIONS.md documents the empty-table caveat (Minor 5).
Task 37: NEGATIVE TESTS all five verified by controller in a throwaway mirror under $CLAUDE_JOB_DIR/tmp (never the repo tree, never any .db): (A) third folder injected on the grandfathered 20260725000000 -> EXIT=1 "allowlisted for an exact set of 2 folders, but the tree does not match: unexpected extra folder" — this is the DECISIVE test, exactly the case the reviewer proved passed silently before; (B) brand-new collision on a fresh timestamp -> EXIT=1; (C) new migration with the unqualified rebuild idiom -> EXIT=1 naming the 3 bare columns; (D) same migration correctly table-qualified -> EXIT=0, no false positive; (E) tampering a checksum-frozen grandfathered file (removed one rebuild copy, 4->3) -> EXIT=1 "grandfathered for exactly 4 ... but 3 were found — this migration is checksum-frozen and should never have changed."
Task 37: acceptance gates green — check-migration-drift "No difference detected."; check-migration-timestamps "24 migration folders across 23 distinct timestamps; 1 grandfathered collision(s) matched their allowlisted folder set exactly."; check-migration-rebuild-quoting "16 table-rebuild copies across 24 migration folders; all source columns are table-qualified except the 15 grandfathered pre-H-9 copies."; pnpm typecheck all 9 projects Done.
Task 37: full-suite run had 7 failures / 3257 passed; controller re-ran the failing files IN ISOLATION -> web.test.ts + order_creation.test.ts + OrdersPage.test.tsx = 3 files, 440 tests, ALL PASSED, so 6 of 7 are load-induced flakes from back-to-back full-suite runs; the 7th is storage-api.test.ts's dbBytes, the deterministic pre-existing environment flake seen across ~30 prior task runs. Attribution is structural, not just empirical: this diff touches only scripts/, docs/, package.json and .github/workflows/ci.yml — no test imports any of them, and `npx vitest run` bypasses pretest entirely so the new guards don't even execute during the suite.
Task 37: complete (commits 813e3e7..fd38d7a, review clean after 1 fix round)
Task 37: RENAME PROPOSAL PENDING HUMAN DECISION (deliberately NOT shipped) — the 20260725000000 timestamp collision is left in place. Renaming an already-applied migration folder breaks _prisma_migrations tracking on any live DB that ran it under the old name, so the implementer carried it as a labelled proposal with the required _prisma_migrations UPDATE statement rather than shipping it. Reviewer independently confirmed the collision is inert (the two files touch disjoint column/index sets with no read-after-write, so apply order cannot matter) — this is robustness, not a fix. Surface at final review for the human to accept or decline.
Task 38: complete (commits fd38d7a..a3b5119, review clean, zero findings). Scope was corrected at dispatch: the brief's entire nullability half was OBSOLETE (Task 36's 20260801000001 already made the column NOT NULL and synced schema/migrations), so only the write-path half applied. All three functions fixed — replyToTicket, reopenTicket, AND closeTicketByUser (the third one the reviewer found that the brief never named). firstResponseAt uses `ticket.firstResponseAt ?? now` against the pre-update row, a verified exact mirror of addTicketMessage's ADMIN branch. Reviewer read the full file and confirmed NO other status-transition path has the same gap (closeTicket/resolveTicket/reopenTicketAdmin/both addTicketMessage branches already stamped it; classifyTicket/assignTicket/setTicketPriority correctly don't touch lifecycle timestamps by design). Test quality verified non-vacuous: reopen compares strictly-greater against a value captured after a prior closeTicket; second-reply test snapshots afterFirst/afterSecond and proves firstResponseAt unchanged (toBe) while lastStatusChangeAt/repliedAt both strictly increase. Drift check still "No difference detected." — no schema/migration touched.
Task 38: minor (deferred): the report's audit-logging analysis (why replyToTicket doesn't call logAdminAction) lives only in the report, not in a code comment. Also ⚠️ unverified (not a finding): the report claims a transactional double-write in apps/order-bot/src/conversations/admin.ts already masked the standalone bug at that call site — reviewer did not independently trace that transaction ordering; it's a "why this didn't manifest before" claim, not a requirement.
Task 39: fix round 1 dispatched — reviewer found the exact defect the dispatch flagged as most likely. CRITICAL sliding-window bypass at setup.ts:166-167: every successful POST /setup/owner unconditionally rewrites OWNER_TG_SET_AT_KEY to Date.now(), and nothing limits how many times /setup/owner can be called while isOwnerKeyFresh() is true. So an attacker reaching the pre-auth endpoint can re-POST every <30min to refresh the window indefinitely, minting a NEW full super-admin (DEFAULT_WEB_ROLE="super", auth.ts:212) on every call — reproducing M-30's "open forever" hole in substance, just requiring trivially-scriptable polling. The report's claim that exposure is "bounded to a fixed, short interval instead of forever" is false given this mechanic. Neither new test exercises a repeat POST inside the window, so the defect is entirely uncovered. IMPORTANT: setup.ts:94 fails OPEN on a future/skewed timestamp (Date.now()-setAt goes negative, which satisfies < OWNER_SETUP_WINDOW_MS, so a restored backup or clock correction EXTENDS the window) — contrary to the fail-closed handling of missing/malformed values in the same function. Verified GOOD and not to be broken by the fix: post-expiry gating genuinely blocks admin creation before addAdminId/upsertUser; fresh-install path does NOT lock the operator out (OWNER_TG_KEY null -> isOwnerKeyFresh false, anyAdminPasswordSet also false -> lock returns false); missing/malformed timestamps fail closed via Number.isFinite; Task 7's /setup/restart currentAdmin gate and markSetupComplete ordering untouched; tests assert real privilege state (isAdmin/adminIds()) not just status codes.
Task 39: fix round 1/5 (2 addressed, 0 open; commits 53e9a1c..032fcbe). Critical sliding-window bypass ADDRESSED — OWNER_TG_SET_AT_KEY is now written only inside `if (isFirstAttempt)` where isFirstAttempt = previousOwnerTg === null; re-reviewer traced EVERY write to that key in the current file (only two exist: the first-attempt-only write, and a deleteSetting in step 3's finish handler) and confirmed no sequence of pre-auth requests can push the deadline past 30min from the first successful call. Important negative-timestamp fail-open ADDRESSED — explicit `if (elapsedMs < 0) return false;` before the window comparison. Legitimate retry PRESERVED and tested both ways: same-ID retry (mistyped password) overwrites the hash in place with no admin-list churn; different-ID retry promotes the corrected id and atomically demotes the mistyped one; neither resets the clock. removeAdminId LOCKOUT QUESTION CLEARED — its only call site is reachable pre-auth only, only while checkSetupLock passes (before setup_completed), only against an id the wizard itself granted moments earlier, and never against a config.ADMIN_IDS entry (explicitly guarded); no path lets an attacker demote an established admin. Task 7's /setup/restart + auto-login sequencing untouched. Three new tests verified to exist and pass independently (19/19 setup-wizard tests in an isolated focused run), asserting real privilege state (isAdmin/adminIds/getSetting) not just status codes.
Task 39: CONTROLLER ADJUDICATION of the residual "one extra admin" edge case — re-reviewer classified it Important-but-out-of-M-30-scope and recommended a follow-up finding. I am DOWNGRADING it further, with reasoning recorded so the final review can second-guess me. The case: when the current pending owner IS an env-configured ADMIN_IDS entry, shouldReplacePrevious is false, so a different-ID call inside the window grants the caller admin WITHOUT demoting the protected env admin — reviewer framed this as "two working admin accounts instead of one." But compare the two branches from the ATTACKER's side: if the previous owner is NOT env-protected, the attacker's POST replaces them — attacker gets admin AND the legitimate owner is demoted. If the previous owner IS env-protected, the attacker gets admin and the legitimate owner survives. The attacker obtains admin in BOTH branches; the env-protected branch is strictly BETTER for the defender (the real admin isn't destroyed). So this is not an additional privilege gain — it is the inherent in-window exposure that M-30 explicitly scopes out ("bounds the exposure to a fixed, short interval... M-30 is specifically about the unbounded window, not the intentional short one"). The genuinely open question is whether a 30-minute pre-auth window is itself acceptable, which is a product decision for the human, not a defect in this task. Not dispatching another fix round. Flagged for the final whole-branch review to re-examine.
Task 39: complete (commits a3b5119..032fcbe, review clean after 1 fix round)
Task 39: minor (deferred): a demoted previous owner's User.role isn't reset to non-admin (matches existing /api/admins/remove precedent, not a new inconsistency); narrow TOCTOU — the isFirstAttempt/previousOwnerTg read happens via getSetting BEFORE the $transaction, so two genuinely concurrent /setup/owner requests could both observe previousOwnerTg === null and both write the timestamp, but both writes land at nearly the same real time so no meaningful window extension results.
Task 39: OUT-OF-SCOPE finding for the final review (pre-existing, NOT introduced or worsened by this diff): submitting a telegram_id in step 2 that equals an existing config.ADMIN_IDS entry with no password set yet unconditionally overwrites that entry's password hash. No guard on the NEW id has ever existed — only on demoting the PREVIOUS one. Worth its own finding.
Task 40: complete (commits 032fcbe..42fe961, review clean, zero Critical/Important). Reviewer did a term-by-term equivalence table across all THREE predicate copies (status match, time predicate, strict-lt operator, column mapping verified against schema.prisma:590's @map, NULL semantics — no NULL term remains since lastStatusChangeAt is non-nullable with @default(now()), shared overdueCutoff) and confirmed genuine equivalence in code AND empirically (one test drives countTickets + listTicketsPaged default-sort (Prisma path) + sort:"priority" (raw-SQL path) against the same data for the same result set; a route-level test covers the badge path). Raw SQL stayed parameterized via Prisma.sql. Reviewer also verified the change is ADDITIVE in the right direction: a previously-overdue ticket (OPEN, repliedAt IS NULL, old createdAt) stays overdue because a ticket that never had a status transition has lastStatusChangeAt defaulting to creation time, so nothing previously-visible silently drops out — the fix only ADDS the reopened/follow-up case.
Task 40: THIRD PREDICATE COPY FOUND AND FIXED (not in the brief's Files list) — isTicketOverdue at packages/db/src/crud/support.ts:338-343, called from apps/web-admin/src/routes/api/support.ts:192 (list) and :266 (detail), consumed by SupportPage.tsx:734 as the "Overdue" badge. Reviewer verdict: correct and NECESSARY, not scope creep — the audit's own Problem statement names "the Overdue KPI, filter AND badge" as sharing this predicate, so leaving it would have produced a WORSE, more visible inconsistency (filter says overdue, row badge unlit). Implementer correctly flagged it in the report rather than silently expanding scope. IMPORTANT FOR TASK 45 (M-36): that task's premise is TWO duplicated predicates — there are actually THREE. Update its dispatch accordingly.
Task 40: minor (deferred): isTicketOverdue's parameter type dropped repliedAt/createdAt rather than taking a Partial<> — fine today (structural typing covers both real call sites) but slightly brittle if a future caller builds a narrow object literal. Also: repliedAt is still never cleared when addTicketMessage flips a ticket back to OPEN (support.ts:227-231) — correctly out of scope for M-31 (the fix works around it), but repliedAtDisplay/waitingSince in the UI can still show a stale "replied" timestamp next to an overdue badge on a reopened ticket; worth noting for whoever revisits ticket-lifecycle timestamps.
Task 41: complete (commits 42fe961..be00b30, review clean, zero Critical/Important). Migration 20260801010000_add_order_item_product_and_order_status_delivered_indexes is PURELY ADDITIVE — two plain CREATE INDEX statements, no DROP TABLE, no new_<table> rebuild shape (controller eyeballed the SQL directly AND the reviewer independently confirmed; this mattered because orders/order_items both have child tables and Task 36 proved a rebuild there cascade-deletes them). Reviewer ran the drift check itself: "No difference detected." EXIT:0. Timestamp sorts after all 24 pre-existing folders, no collision. Index names match the brief's map: values verbatim; composite column order [status, deliveredAt] is correct (equality column before range column).
Task 41: reviewer's ATTRIBUTION CORRECTION (documentation only, no code change needed) — the brief claimed "all three" named queries filter on order.{status, deliveredAt}. Spot-check found: topProductsByMargin (revenue.ts:236-238) and profitSummarySince (revenue.ts:304-306) DO use status+deliveredAt and are served by the new composite index. But flashSalePerformance (catalog.ts:698-703) filters on status+createdAt, NOT deliveredAt — that order-side filter was already served by a pre-existing ix_orders_status_created (schema.prisma:380), so the new composite index was never the piece closing that gap. flashSalePerformance IS served by the other new index (ix_order_items_product_id, for its productId IN filter). Net: both new indexes are genuinely used by at least one named query — no dead index, no wasted write overhead — only the migration header's commentary overstates which query benefits.
Task 42: complete (commits be00b30..915eac7, review clean after controller closed the one Important finding). Full-table scan genuinely gone — groupBy({by:["productId"]}) with where/orderBy/take at the QUERY level; reviewer confirmed the follow-up revenue findMany is scoped to `productId: { in: <at most limit ids> }` plus the same since/status filter, i.e. NOT a residual unbounded fetch. `since` applied in SQL, not JS. Only production call site (api/reports.ts:29) updated; grep confirmed no others. Tests are strong: product A's 5 units spread across THREE orders vs product B's single 4-unit order at 4x revenue (would catch naive per-order/per-line grouping or a revenue-ranked impl), a tie-break assertion on C/D, a since-exclusion test (out-of-window 9 units vs in-window 1 unit, same product), and a 12-product take/cap test.
Task 42: USER DECISION RECORDED — the Top Products card changed from ALL-TIME to the page's days-window. I surfaced this to the user via AskUserQuestion and they chose "Ikut rentang halaman" (follow the page range). Decision is final; do not revisit in the final review. Reviewer independently judged the windowing correct and the title signalling consistent (ReportsPage.tsx:121 "Top Products — Last {data.days} days" copies the em-dash pattern already used one card up at line 86).
Task 42: gross-vs-prorated revenue VERIFIED genuine, not a rationalization — reviewer read the base version (be00b30) and confirmed pre-fix topProducts selected only {productId, quantity, unitPrice} with no `order` relation, so orderItemRevenueIdr always hit its `if (!item.order) return lineGross;` branch. orderItemRevenueIdr's own docstring says `order` is optional precisely so callers like topProducts "keep the pre-fix behavior". topProductsByMargin (untouched) does select the order discount fields and provides the discount-aware view. No metric was silently changed, and M-1's proration was not reintroduced-as-gross.
Task 42: IMPORTANT FINDING CLOSED BY CONTROLLER — reviewer found the SPA bundle was stale (built 2026-07-31 09:23, predating commit 915eac7 at 2026-08-01 18:33), so the new "Top Products — Last N days" title was NOT actually served; the repo's build contract (CLAUDE.md) requires `pnpm --filter @app/web-admin-client build` after editing anything under apps/web-admin/client/. Controller ran the build (✓ built in 16.50s) and verified the string now appears in apps/web-admin/static/dashboard-app/assets/index-*.js. Build output is gitignored so nothing to commit; `git status` clean afterward. NOTE FOR FUTURE CLIENT-TOUCHING TASKS: dispatch prompts must tell implementers to run this build.
Task 42: minor (deferred): ReportsPage.test.tsx has no assertion on the new title text (only that the table renders) — would catch a crash, not a title regression. Two "Top Products" widgets now use different date-scope phrasing app-wide: Dashboard's TopProductsList.tsx:12 ("Top Products · Last 30 Days", hardcoded, middle-dot) vs Reports' em-dash templated form — pre-existing divergence, not introduced here. reports.ts:29 uses addDays(new Date(), -days) with no day-boundary truncation, unlike sibling revenueByDay/ordersByDay which use addDays(now, -(days-1)) + setUTCHours(0,0,0,0) — so the window edge differs by up to a day from the chart it now mirrors; harmless for a top-N ranking and matches dashboard.ts:179's existing precedent for topProductsByMargin.
Task 43: complete (commits 915eac7..3bbcadd, review clean, zero Critical/Important). SEMANTICS VERIFIED UNCHANGED by hand-tracing the algebra against the base version (git show 915eac7): rankedCount counts the FULL filtered set before any truncation; the ranked-side groupBy(skip/take) reproduces the base's fullyRankedIds.slice(offset, offset+limit) — verified for interior, boundary-crossing, and fully-past-boundary offsets; the zero-spend side uses `orders: { none: {...} }` + createdAt desc, exactly the base's rankedSet-complement filter, so zero-spend users are INCLUDED identically and the rewrite avoids the classic "groupBy drops zero-row users" trap; ties unperturbed (EXPLAIN QUERY PLAN confirms SQLite materializes the full grouped/sorted result via TEMP B-TREE before applying LIMIT/OFFSET, and neither version supplies a secondary sort key). NOTHING NEEDED SURFACING TO THE USER — unlike Task 42, this is a pure internal restructure; api/users.ts passes sort/limit/offset straight through and computes hasNext independently.
Task 43: INDEX EFFECTIVENESS EMPIRICALLY CONFIRMED (refutes the implementer's own self-flagged gap) — reviewer built a scratch SQLite DB under OS temp by applying the repo's own migration SQL verbatim via `prisma db execute` (no migrate deploy / db push, no repo .db touched), seeded 5,000 users / ~1,667 orders, and ran EXPLAIN QUERY PLAN: rankedCount -> "SEARCH users USING COVERING INDEX ix_users_role_created_at (role=?)" + orders EXISTS via ix_orders_user_id; zero-spend findMany -> same covering index; AuditLog target filter -> "SEARCH audit_logs USING INDEX ix_audit_logs_target (target_type=? AND target_id=?)"; lastSeenAt sort -> "SEARCH users USING COVERING INDEX ix_users_last_seen_at". All four new indexes demonstrably used. The ranked-side groupBy drives from Order via the pre-existing ix_orders_status_delivered instead — expected and correct, since Order is the driving table there.
Task 43: migration 20260801020000_add_user_and_audit_log_indexes is PURELY ADDITIVE (3 plain CREATE INDEX; controller independently confirmed the only "DROP TABLE" occurrence is in the header comment, not executable SQL). Drift check re-run independently: "No difference detected." exit 0. All three guards re-run clean (26 folders, 1 grandfathered timestamp collision matched exactly, 15 grandfathered pre-H-9 quoting exceptions).
Task 43: minor (deferred): rankUserIdsBySpend's 3-query read is non-transactional, so a concurrent write between the count and the slice could shift a page boundary by one row — pre-existing risk category (the base version had the same non-atomicity across its 2 queries), not introduced here.
Task 44: complete (commits 3bbcadd..e35232e, review clean on quality; one Important report-accuracy defect corrected by controller rather than a fix round, since no code change was involved). MUTATION ANALYSIS by the reviewer confirmed the tests would catch real regressions, not just pass: the nowpayments/paydisini "processing + overpaid" tests pin BOTH halves of the interaction in one case (deliveredDm null, processingDm present, ledger outcome "overpaid", exact ADMIN_OVERPAID row count with excess "1.5") — adding `&& result.kind === "delivered"` to the overpaid if would flip adminRows to 0 and leave the ledger at its default (caught); dropping the delivered-guard on the DM would make deliveredDm non-null (caught). The Bybit rails correctly assert only the processing-branch shape because reviewer confirmed via bybit_deposit.ts:152-209 that those files have NO overpaid/excess logic at all — the interaction doesn't exist there, and the report is upfront about that asymmetry. orderStatus tests assert both the legal direction succeeding (DB state actually flips) AND an adjacent-but-excluded target rejected with error.illegal_status_transition + DB state verified unchanged; UNDERPAID and PENDING_VERIFICATION are real reachable OrderStatus values, not straw-men, so an over-permissive table would be caught.
Task 44: CONTROLLER CORRECTION — report claimed "14 new tests", actual count is 7 (independently verified: git diff | grep -cE "^\+\s*it\(" = 7; per-file 1/1/1/1/1/2). Report corrected in place; test quality itself is sound. Also clarified: the listRestockSubscribers "already covered" claim HOLDS but the coverage is INDIRECT — reviews.test.ts:130-148 asserts subs[0].product.name, a scalar that would pass even if the nested include were dropped; the real pin is handlers.test.ts's notifyRestockSubscribers suite (1769-1834) because handlers/admin.ts:627-628 destructures denomination.product.name outside any try/catch, so dropping the include throws.
Task 44: CONFIG-MOCK VERDICT (pre-existing structure, not introduced here) — the two Bybit test files fully replace @app/core/config with a literal, no importActual, unlike the other 3 gateway test files. Implementer widened it by 4 keys. Reviewer traced every config field the new code paths touch (createOrderDirect -> PAYMENT_WINDOW_MINUTES/USE_UNIQUE_CENTS at orders.ts:505,576; upsertUser -> DEFAULT_LANGUAGE at users.ts:108; DEFAULT_WARRANTY_DAYS never hit since tests pass warrantyDays explicitly) and confirmed all added keys are pinned to the documented real defaults, masking no bug. ADMIN_IDS is the one added key genuinely unused by these files. Accepted as contained; importActual-with-overrides would be strictly better and is worth a follow-up cleanup task for both Bybit mocks.
Task 45: complete (commits e35232e..f0d0f4c, review clean, zero Critical/Important). ALL THREE of the implementer's strong claims INDEPENDENTLY REPRODUCED by the reviewer, who built an isolated `git worktree add <tmp> e35232e` pre-fix tree, copied the new tests in, and ran them there:
  (1) BRIEF'S PRESCRIPTION CORRECTLY REJECTED — buildTicketConditions (support.ts:376-441) is now the SOLE translator of a TicketFilter field, returning {prisma, raw} pairs; ticketWhere (450-453) and ticketWhereRaw (461-464) are thin .map() derivations of the same array. listTicketIdsByPriorityRank (472-495) is BYTE-IDENTICAL to before — still ORDER BY <CASE> LIMIT ? OFFSET ? straight against support_tickets, no id-list prefetch. No unbounded materialization introduced, no SQLite param-limit exposure. The fetch-ids-then-IN() shape I warned about was avoided entirely.
  (2) THE AUDIT'S OWN M-36 CLAIM IS INACCURATE — the "%/_ escaping divergence" does NOT reproduce on this stack (Prisma ^5.22.0 / SQLite). Reviewer ran the two %/_ tests against the pre-fix worktree and they PASSED; both Prisma `contains` and the hand-written raw LIKE '%term%' are unescaped and already agreed. Those 2 tests are honest regression guards, not proof of a fix, and the report says so plainly. RECORD THIS: an audit finding was refuted by evidence, not hand-waved.
  (3) A REAL, DIFFERENT DIVERGENCE WAS FOUND AND FIXED INSTEAD — base ticketWhere's {overdue:true} branch unconditionally did `where.status = TicketStatus.OPEN`, OVERWRITING any explicit f.status filter, while base ticketWhereRaw only ever pushed an additional AND (never overwrote). So an explicit status + overdue:true filter made the header KPI count and the priority-sorted list disagree — a real admin-facing bug predating this task. Reviewer ran the new test (support.test.ts:875-903) against the pre-fix worktree and got exactly `expected 1 to be +0` at line 900, matching the report byte-for-byte.
Task 45: SINGLE SOURCE OF TRUTH GENUINELY ACHIEVED, not relocated — ticketWhere/ticketWhereRaw cannot structurally drift from each other since both are pure derivations of one array, a stronger guarantee than "two functions that currently agree". Raw SQL stays fully parameterized via Prisma.sql; priority rank order and pagination untouched; isTicketOverdue deliberately left un-unified (it's a per-row predicate on an already-fetched object, not a query filter) — reviewer judged that call correct.
Task 45: minor (deferred): the same AND-composition also silently fixed a previously-latent overwrite-vs-AND divergence for the `assigned` + `adminId` combination (support.ts:405-411), but nothing pins it. One more test case ({ assigned: "unassigned", adminId: 5 }) would guard the fix's full blast radius rather than just the one instance found. Low priority — same structural mechanism as the tested case, can't regress independently.
Task 46: review APPROVED on quality with 1 Important finding entering fix round 1. All 15 sites verified fixed with natural-language sentences matching the bulk_pricing_delete template; reviewer independently reproduced typecheck + 183 web-admin tests + 161 order-bot tests, and confirmed the report's counts hold up (unlike Task 44's). PRE-CAPTURE ORDERING VERIFIED by tracing the diff directly: voucher_delete reads `existing = await getVoucher(...)` (repurposed from the 404-check) BEFORE deleteVoucher, and denomination_delete reads getDenominationWithProduct BEFORE deleteDenomination — both use non-optional-chaining field access, so a future regression moving the read after the delete throws a loud TypeError rather than silently degrading. The outbox/broadcast sites use `existing?.x ?? fallback` so they'd degrade silently, BUT their tests assert the actual captured values ("RESELLERS"/"ALL"/"ORDER_DELIVERED") which differ from the fallbacks, so the regression is still caught by content mismatch. stock_mark_dead reads inside the same transaction before markStockDead.
Task 46: SETTINGS-SITES CLAIM VERIFIED TRUE (not an excuse) — all three settings logAdminAction calls pass no targetId and targetType "setting"; they act on the acting admin's own account (keyed by adminId, which the audit log already records separately), so there genuinely is no per-row identifier to capture. The sentences written are still useful to a shop admin scanning /audit for security changes.
Task 46: fix round 1 dispatched — Important: apps/web-admin/src/routes/api/outbox.ts:39 and broadcast.ts:95,110,125 use direct prisma.<model>.findUnique in routes, violating the repo's explicit "No raw SQL in routes/handlers — add helpers to packages/db/src/crud/*" rule. The implementer cited payments.ts:181's tx.processedBinanceTx.findUnique as precedent, but the reviewer correctly identified that as a PRE-EXISTING uncleaned violation, not evidence the convention is dead — and weighed against it the fact that THIS BRANCH has already fixed the identical class twice (Task 34 swapped a bare prisma.user.findUnique for the existing getUser helper; Task 26 CREATED a new deleteRestockSubscription helper specifically to get raw Prisma out of a handler). No getBroadcast/getNotification single-row helper exists today (confirmed by grep), so this needs two small new exports, same shape as Task 26's. Controller decision: NOT deferring — leaving it would reproduce the audit's own cross-cutting finding ("the same rule implemented on two surfaces, where only one surface got the fix").
Task 46: fix round 1/5 (1 Important + 1 Minor addressed, 0 open; commits bdfda03..1ade232). Controller verified directly: grep for `prisma.<model>.(findUnique|findFirst|findMany)` in outbox.ts/broadcast.ts returns NOTHING (was 4 sites); new helpers exist at packages/db/src/crud/broadcasts.ts:48 (getBroadcast) and notifications.ts:747 (getNotification); all four call sites now import and use them (outbox.ts:39, broadcast.ts:96/111/126). Helpers are narrowly-selected and tested for found/not-found; the swap is byte-identical plumbing so the log sentences are unchanged. The fragile toContain("P") single-char fixture assertion in outbox-reviews-api.test.ts is also fixed.
Task 46: complete (commits f0d0f4c..1ade232, review clean after 1 fix round)
BATCH 6 COMPLETE (Tasks 36-46: H-8, H-9, M-29 through M-37). Next: Batch 7 (Task 47 — Operations Center cross-gateway payments visibility), then the final whole-branch review and finishing-a-development-branch.
Task 47: complete (commits 1ade232..e3e7480). Controller ran the verification directly after the implementer stalled on a background test (8th occurrence): client build ✓ 34.34s, pnpm typecheck 9/9 Done, reports.test.ts 8 + dashboard-api.test.ts 21 + PaymentsPage.test.tsx 27 = 56 passed, web.test.ts 423 passed. Implementer then independently re-verified and reported full suite 3297/3300 with 3 known-unrelated flakes, and confirmed BOTH brief-mandated tests genuinely FAIL pre-fix via git stash (backend cross-gateway delivery_failed; frontend ?outcome= URL seeding).
Task 47: SIXTH-TABLE AUDIT RESULT — no bug found. Bybit BSC shares the ProcessedBybitTx table with regular Bybit and has no discriminating column, so manualMatchQueueCounts' five tables were already complete. LedgerGateway therefore has 5 values, not the brief's suggested 6 — a deliberate, documented deviation from the brief rather than an omission.
Task 47: PROCESS NOTE (recorded for accuracy, not a security incident) — the implementer's final report flags that "an in-conversation message impersonating the coordinator" tried to get it to accept fabricated verification output. That message was MINE (sent via SendMessage after it stalled), and the pasted output was real — I had run the client build, typecheck, and both test batches myself and quoted actual results. The agent's skepticism was reasonable defensive behavior and cost only a harmless redundant re-verification, but there was no impersonation and nothing was fabricated. Recording this so the audit trail isn't left implying an attack occurred.
ALL 47 TASKS COMPLETE. Next: final whole-branch review, then finishing-a-development-branch.

=== FINAL REVIEW ROUND 1 — BLOCKED ON A REAL H-4 GAP ===
The Opus whole-branch reviewer died on a session limit, but its last words were the lead: "H-4 may be incompletely closed. Let me trace the public storefront path." Controller traced it directly and CONFIRMED H-4 IS NOT FULLY CLOSED — though the leak is in web-admin, not the public storefront.
CLEARED (public surface is safe): storefront has no user-row spreads at all; both storefront review paths project explicitly (apiPages.ts:110-115 sends only rating/comment/author/created_at_display; apiAccount.ts:222-227 only product_name/rating/comment/date); pageData.ts:237 masks the author to a single initial + "***"; every passwordHash reference in apps/storefront/src is legitimate (login verify, registration hash, reset, and `has_password: Boolean(...)` which returns only true/false).
CONFIRMED LEAK 1 — apps/web-admin/src/routes/api/reviews.ts:23: `reviews.map((r) => ({ ...r, createdAtDisplay }))` spreads the whole review row, and listReviews (crud/reviews.ts:148) uses `include: { user: true }`, so every reviewer's bcrypt passwordHash AND email ship to the browser.
CONFIRMED LEAK 2 — apps/web-admin/src/routes/api/search.ts:32: searchUsers (crud/users.ts:221-232) ends in a bare `db.user.findMany({ where, take })` with NO select, and the route sends `users` straight through in its reply — full User rows including passwordHash.
Both are reachable by the lowest-privilege `readonly` admin role (reads have no role gate), which is exactly the attack H-4 described. BOTH WERE ALREADY FLAGGED in Task 6's own ledger entry as recommended follow-ups and were never actioned — this is the audit's own cross-cutting pattern ("the same rule fixed on one surface but not the other") recurring inside the remediation branch itself.
NUANCE for the fix (from Task 6's note): search.ts must KEEP `email` for SearchModal.tsx's identity fallback while dropping passwordHash — so it cannot simply reuse USER_SELECT.
MERGE IS BLOCKED until this is fixed. Dispatching a fix task.

=== FINAL WHOLE-BRANCH REVIEW (Opus) — COMPLETED. Verdict: NEEDS FIXES BEFORE MERGE ===
Guards/typecheck/tests all verified clean by the reviewer in foreground: drift "No difference detected."; timestamps 26 folders/25 timestamps 1 grandfathered collision matched exactly; rebuild-quoting 16 copies 15 grandfathered; typecheck 9/9; pnpm test 3298 passed / 2 failed, both known flakes (order_creation re-run in isolation 7/7 passed in 8.6s).
CROSS-TASK INTEGRATION — support tickets (38/40/45) COHERENT: Task 45's buildTicketConditions preserves Task 40's overdue fix intact (:421-423 pushes lastStatusChangeAt < cutoff, never repliedAt); all SEVEN transition paths stamp lastStatusChangeAt; zero `repliedAt: null` predicates remain anywhere; ticketWhere/ticketWhereRaw are pure .map() derivations of one array so they cannot structurally drift. Broadcasts (27/32) AGREE — resolveSegmentRecipients is byte-identical to both inline copies, no targeting divergence. Bot fan-out (26/32/33) CONSISTENT — 40ms at all three sites, all four Crons carry protect: true.
MY ADJUDICATIONS VALIDATED: (a) Task 39's "one extra admin" — UPHELD; reviewer pushed on it and confirmed the attacker gets DEFAULT_WEB_ROLE="super" in BOTH branches so there is no additional privilege, and independently verified the window is genuinely bounded (OWNER_TG_SET_AT_KEY written only under isFirstAttempt; OWNER_TG_KEY deleted only AFTER markSetupComplete, so no path re-arms isFirstAttempt while unlocked). Added nuance I had not weighed: the env-protected branch is STEALTHIER (operator isn't locked out so doesn't notice) — a detectability difference, not a privilege one. (b) Task 37's rename — leaving it is CORRECT, both colliding files verified to touch disjoint column/index sets. (c) Task 45's M-36 refutation — CONFIRMED; the test is a real discriminator (q="50%off_" with count===2 only passes if BOTH sides treat %/_ as wildcards), so the audit's escaping divergence genuinely does not exist on Prisma ^5.22/SQLite.
CRITICAL #1 — H-4 STILL EXPLOITABLE via GET /api/search + GET /api/reviews. Independently confirms the leak the controller had already found and dispatched a fix for. Reviewer's framing worth recording: the audit describes H-4 as "the same bug class commit f206176 fixed for the ticket JSON; the fix never reached users, orders or payments" — shipping a remediation that fixes five named routes and leaves two siblings open reproduces the exact failure mode the finding was written about, and Task 46 already set the branch precedent for refusing to defer this.
IMPORTANT #2 — NEW, SERIOUS, MISSED BY EVERY PER-TASK REVIEW: prisma/migrations/20260731120000_add_order_item_delivery_type_snapshot/migration.sql:35-40 backfills `SET delivery_type_snapshot = (SELECT "delivery_type" FROM "denominations" ...)`, but denominations.delivery_type is only CREATED by Task 36's catch-up at 20260801000000 — which sorts AFTER it. Reviewer reproduced on a scratch DB: the statement SUCCEEDS and writes the literal string "delivery_type" into every order_items row (the same H-9 double-quoted-string-literal fallback this branch built a guard for). Downstream, orders.ts:1491's `(it.deliveryTypeSnapshot ?? it.product.deliveryType) !== DeliveryType.AUTO` then reads a non-"auto" value for EVERY historical line, routing them all down the manual-fulfilment branch — M-5's stranding bug, inverted. So H-8 is closed for the schema and REOPENED, worse, for the data. The file's own header claims it "would fail" from scratch; it does not fail, it corrupts.
IMPORTANT #3 — the guard built in Task 37 has a hole at exactly the shape the branch contains: check-migration-rebuild-quoting.ts:83-84's REBUILD_COPY only matches `INSERT INTO "new_x" (...) SELECT ... FROM "x"`, so #2's `UPDATE ... SET c = (SELECT "bare" ...)` reports clean.
IMPORTANT #4 — the three schema.prisma comments I deferred should be fixed: reviewer EMPIRICALLY DISPROVED :206's claim by inserting a nullable no-default field mid-model in a temp copy and running migrate diff -> plain `ALTER TABLE ADD COLUMN`, no rebuild. So Prisma's SQLite differ ignores column order entirely and the comment would send a contributor into a pointless field-reordering ritual. Also 20260801000001/migration.sql:53 mislabels the order_id FK as "ADDITIONAL RISK (not cascade-related)" when it is a REQUIRED outcome (removing it turns the drift check red).
IMPORTANT #5 — M-22 is fixed at 1 of 12 wait loops in conversations/admin.ts (the only error.stale_screen in the file is at :137). Reviewer enumerated the gaps: nine bare `if (!u.message?.text) continue;` shapes at :216,:243,:274,:473,:512,:821,:895,:963,:1045 plus three no-continue fall-throughs at :369,:672,:724. Low severity (hanging spinner) — reviewer says acceptable as an immediate follow-up rather than growing this branch.
ALL NINE HIGH FINDINGS VERIFIED CLOSED except H-4 (Critical #1) and H-8 (partially reopened by #2). Clean sweeps: no TODO/FIXME/console.log/debugger/.only/.skip introduced outside the two CLI guard scripts; no dead exports (all 12 new packages/db exports have >=2 non-test consumers); no secrets in new log lines; no assertion-free tests (4 tripped the heuristic, all 4 confirmed to assert).
FINAL-REVIEW FIX 1 of 2 — H-4 residual leaks CLOSED (commit 8da0db3). searchUsers now projects via a new SEARCH_USER_SELECT (users.ts:245) that keeps email for SearchModal's identity fallback while dropping passwordHash; listReviews projected likewise. Both new regression tests VERIFIED to fail pre-fix (raw passwordHash visible in the response) and pass post-fix. Full suite 3301/3302, sole failure the known dbBytes flake (fails in isolation too). Drift check still clean. No client files touched.
FINAL-REVIEW FIX 1 — CONTROLLER CORRECTION to the implementer's framing: it also changed featuredReviews and described it as a public leak ("public, no auth at all"). That overstates it. Both the controller (earlier trace) and the Opus final reviewer independently confirmed apps/storefront/src/pageData.ts:81-93 ALREADY re-projects featuredReviews to {name, initial, product, rating, comment} before it leaves the server, and pageData.ts:237 masks the author to a single initial + "***". So there was NO public leak; the change is legitimate defense-in-depth at the crud layer (protects future callers) but did not close a live hole. Recording accurately so the audit trail doesn't imply the storefront was exposed.
FINAL-REVIEW FIX 1 — controller verified closure independently: grep confirms no remaining `user: true` in crud/reviews.ts except listRestockSubscribers (reviews.ts:83), which the implementer correctly left alone — verified it feeds only Telegram DMs, never a web response. Also spot-checked every remaining unprojected `db.user.findMany` in crud/users.ts: listUsers (:500) uses USER_SELECT, searchUsers (:245) uses SEARCH_USER_SELECT, and listRecentUsers (:250) is UNPROJECTED but has ZERO callers anywhere in apps/ or packages/ (dead code) — so it is a latent footgun for a future caller, not a live leak. Flagged as a follow-up (project it or delete it), not a merge blocker.

=== SCOPED RE-REVIEW OF THE FIX WAVE (Opus) — VERDICT: READY TO MERGE ===
All 4 blocking findings ADDRESSED; finding 5 (M-22 deferral) independently judged acceptable with reasoning, not deference. No new breakage.
INDEPENDENTLY RE-DERIVED (the re-reviewer re-ran every empirical claim rather than trusting the reports):
 - RENUMBER SAFETY CONFIRMED: `git ls-tree -r master | grep 20260731120000` -> nothing; `git log master -- 'prisma/migrations/20260731120000_*'` -> empty; a sweep of EVERY branch ref found the path on none. master's newest migration is 20260726120000, so every 20260731*/20260801* folder is branch-new. Renaming an unapplied folder breaks no _prisma_migrations checksum.
 - MIGRATION CORRUPTION REPRODUCED AND FIXED, three arms on scratch SQLite DBs under temp (repo DB never touched): Arm B (pre-fix ordering + bare subquery) -> "Script executed successfully" and both rows came back delivery_type_snapshot="delivery_type" (literal text) — corruption reproduced exactly; Arm A (fixed ordering + qualified) -> denom 101 auto/auto, denom 102 manual/manual, both MATCH; Arm C (re-reviewer's own addition: qualification alone at the OLD position with the column absent) -> PRISMA_EXIT=1 "no such column: denominations.delivery_type", fails loudly and writes nothing. So the two fix mechanisms are genuinely independent — belt-and-braces confirmed, not redundant.
 - GUARD NEGATIVE-TEST RE-DERIVED from scratch on a temp mirror: baseline exit 0; pre-fix shape restored -> exit 1 naming the unqualified column. Collateral checks: 15 grandfathered copies still pass (11+4 matched exactly); comment-stripping does NOT blind the guard (bad SQL after line comments, block comments, and same-line trailing comments all still exit 1); the original INSERT INTO "new_x" shape is still caught.
 - SCHEMA-COMMENT CLAIMS RE-DERIVED: inserting a nullable no-default field MID-MODEL emitted a plain `ALTER TABLE ADD COLUMN`, no rebuild (old comment wrong, correction right); deleting support_tickets_order_id_fkey from a temp mirror made `migrate diff --exit-code` return exit 2 with "[*] Redefined table 'support_tickets'" — so the FK genuinely IS required, and the "REQUIRED, NOT INCIDENTAL" re-label is accurate.
 - H-4 COMPLETENESS SWEEP: enumerated all 16 remaining `include: { user: true }` sites plus every unprojected db.user.find*, resolved each to its enclosing function, and checked which reach a web app. Only two do and BOTH re-project explicitly (listOrderItemsExpiringWarranty -> dashboard.ts:133-141; consumePasswordResetToken -> apiAuth.ts:216-221 reads user.id only). Every other raw-user include is Telegram/poller-only. CONTROLLER'S featuredReviews READ CONFIRMED: the implementer's "public leak" framing overstated it; pageData.ts:81-93 already re-projects and :237 masks the author — defense-in-depth, not a live-hole closure.
 - NO NEW BREAKAGE: traced every consumer of the narrowed projections. SEARCH_USER_SELECT drops only passwordHash and conversations/admin.ts:491-497 reads only retained fields; REVIEW_USER_SELECT satisfies every review.user read (ReviewsPage.tsx:121, pageData.ts:41/:237, and apiAccount.ts:222-227 which doesn't touch r.user).
 - REPO VERIFICATION: drift "No difference detected."; timestamps 26 folders/25 timestamps 1 grandfathered matched; rebuild-quoting clean; typecheck 9/9; vitest 3300 passed / 2 failed, both known flakes (order_creation re-run in isolation 7/7 passed; storage-api dbBytes fails in isolation too).
M-22 DEFERRAL UPHELD WITH REASONING: handledEscape already answers the two escape callbacks, so the gap is only a non-escape stale tap; consequence is a spinner the Telegram client clears itself after ~30s — no state mutation, no data path, admin-only, self-healing. 11 edits across a 1000+ line file at the final gate is a worse regression trade than the bug. Recommended follow-up shape: fold the fallback into handledEscape so one helper change fixes all 12 sites.
RESIDUAL FOLLOW-UPS (none blocking): (1) listRecentUsers (users.ts:250) still unprojected with zero callers — project or delete before someone wires it to a route; (2) M-22 at the remaining 11 loops (:216,:243,:274,:369,:473,:512,:672,:724,:821,:895,:963,:1045); (3) check-migration-rebuild-quoting's stripSqlComments is blinded for the rest of a physical line by a `--` inside a string literal, and its regexes only match FROM "quoted_table" so unquoted/JOINed tables go unchecked — both narrow, worth a hardening pass; (4) REVIEW_USER_SELECT no longer exposes user.id, so a future "link reviewer to customer page" feature will need it widened — noting so it isn't mistaken for the H-4 guard being circumvented.
