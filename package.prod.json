{
  "name": "telegram-order-bot-server",
  "version": "0.1.0",
  "private": true,
  "description": "Combined single-process build (dist/server.cjs from `pnpm build:bundle`). Upload this as package.json (rename) alongside dist/server.cjs for a plain-node managed-hosting deploy.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node dist/server.cjs",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "nunjucks": "^3.2.4",
    "pino": "^9.5.0",
    "prisma": "^5.22.0",
    "thread-stream": "^3.1.0"
  }
}
